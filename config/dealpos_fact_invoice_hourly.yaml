schedule: "0 * * * *"

tasks:
  - name: fact_invoice_hourly
    type: etl
    plugin_module: dealpos_etl
    loader_key: fact_invoice
    dag_prefix: dealpos
    mode: incremental
    lookback_days: 1
