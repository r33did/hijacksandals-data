schedule: "0 * * * *"

tasks:
  - name: fact_inventory_hourly
    type: etl
    plugin_module: dealpos_etl
    loader_key: fact_inventory
    dag_prefix: dealpos
    mode: incremental
    lookback_days: 1
